spring:
  liquibase:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: none
#    generate-ddl: true
#    defer-datasource-initialization: true
  sql:
    init:
      mode: always
#  datasource:
#    initialization-mode: always
